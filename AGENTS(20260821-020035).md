# AGENTS.md — Grandia III PS2 일본판 한국어화 프로젝트

## 0. 이 파일의 역할

이 파일은 이 저장소에서 작업하는 모든 Codex 세션이 공통으로 따라야 하는 **프로젝트 상위 지침**이다.

새 세션에서 프로젝트 구조를 처음부터 다시 추측하지 말고, 우선 다음 기존 자료를 읽는다.

```text
Grandia3_KOR_REFERENCE_PACK_2026-08-19
docs/session/
```

특히 `Grandia3_KOR_REFERENCE_PACK_2026-08-19`는 기존 리버스 엔지니어링 결과, 도구, CSV, codebook, MDZ/MDT 분석 및 과거 PCSX2 실험을 담은 기준 자료다.

이 프로젝트의 최종 목표는 **Grandia III 일본판 Disc 1을 실제 PCSX2에서 안정적으로 한국어화하는 것**이다.

연구 보고서 자체가 목적이 아니다.

---

# 1. 증거 우선순위

문서끼리 내용이 충돌하면 다음 순서를 따른다.

```text
1. PCSX2 실제 인게임 화면에서 확인된 결과
2. 최종 ISO에서 역추출하여 byte-identical 검증된 결과
3. 직접 재현 가능한 binary evidence
4. 최신 분석 문서
5. 오래된 가설
```

발견 사항은 다음으로 구분한다.

```text
PROVEN
SUPPORTED
REJECTED
UNPROVEN
```

PCSX2에서 반박된 과거 가설을 다음 세션에서 다시 PROVEN처럼 사용하지 않는다.

---

# 2. 기존에 해결된 것을 재발견하지 말 것

## MDZ / MDT

`.MDZ`는 일반 ZIP/zlib가 아니라 기존 프로젝트에서 해제/재압축 가능한 변형 LHA/LZH Level-2 계열이다.

기존 unpack/pack 도구를 우선 사용한다.

기본 검증 흐름:

```text
clean MDT
→ patch
→ MDZ pack
→ MDZ unpack
→ patched MDT와 byte-identical 확인
```

압축 형식을 처음부터 brute-force하지 않는다.

## GR3 아이템

기존 분석 기준:

```text
Item table base = 0xD80
Record size     = 0x20
record          = 0xD80 + item_id * 0x20

+0x04 = relative name pointer
+0x08 = name byte length
+0x10 = relative description pointer
```

Item 107:

```text
やくそう
raw = 96 62 70 59 00
```

아이템 이름/설명 관련 기존 CSV/JSON/codebook을 다시 처음부터 추출하지 않는다.

## Custom Encoding

GR3 텍스트는 Shift-JIS로 단정하지 않는다.

기존에 확인된 예:

```text
96       = や
62       = く
70       = そ
59       = う
8A F0    = 家
36 F0    = 攻
9E F0    = 草
```

다음 실기 결과는 PROVEN이다.

```text
Item 107:
8A F0 62 70 59 00
→ PCSX2: 家くそう
```

임의의 미등록 code를 신규 한글 문자라고 단정하지 않는다.

---

# 3. ISO 작업 규칙

ISO 전체 rebuild는 우선 사용하지 않는다.

정상 부팅이 확인된 ISO 사본에서 대상 파일만 교체한다.

```text
known-good ISO
→ COPY
→ Grandia III (KOR) (Disc 1).iso
→ modified target file replace
```

최종 테스트 전에 반드시:

```text
KOR ISO
→ modified file 역추출
→ MDZ라면 unpack
→ 작업본과 byte 비교
```

한다.

ISO를 생성하거나 교체하기 전에는 사용자의 최종 승인을 다시 받는다. 승인 전에는 사전검증과 패치 후보 준비까지만 수행한다.

### 누적 통합 빌드 원칙

작업 중인 ISO에 변경 파일만 순차적으로 덧대는 incremental 방식은 사용하지
않는다. 각 전문 세션의 표준 CSV와 후보 산출물을 중앙 세션에서 CLEAN 원본
기준으로 누적 병합하고, 모든 대상 리소스를 포함한 하나의 replacement plan을
만든 뒤 ISO를 한 번만 생성한다.

```text
CLEAN 원본 ISO
→ 중앙 통합 빌드 디렉터리
→ FIELD / BATTLE / GR3.MDZ / DATA 전체 후보 누적
→ 전체 replacement plan
→ repack ISO 1회 생성
→ 전체 payload 역추출·해시 검증
```

이 방식은 번역으로 파일 크기가 증가하는 것을 전제로 한다. 이전 테스트 ISO를
새 빌드의 입력으로 재사용하거나, 일부 파일만 교체한 ISO를 다음 통합의
기준본으로 승격하지 않는다.

## ISO 통합 범위 게이트

ISO 후보는 일부 세션의 결과만 선택적으로 넣어 “통합본”이라고 부르지 않는다. 최소한 다음 번역 영역을 모두 입력 목록과 glyph 집계에 포함한다.

```text
SYSTEM / STATUS / ITEM / BATTLE / SKILL_EFFECT / BATTLE_HELP / CHARACTER / ENEMY / SCENARIO / NPC_DIALOGUE / FIELD_NAME
```

특히 다음 신규 산출물은 필수 입력이다.

```text
exports/enemy_names_standard.csv
exports/enemy_names_required_glyphs.txt
exports/field_names_standard.csv
exports/field_names_required_glyphs.txt
exports/field_names_glyph_request.csv
exports/battle_presentation_help_standard.csv
exports/battle_presentation_help_required_glyphs.txt
exports/special_skill_effects_standard.csv
exports/special_skill_effects_required_glyphs.txt
exports/character_names_standard.csv
exports/character_names_required_glyphs.txt
exports/npc_dialogue_standard.csv
exports/npc_dialogue_required_glyphs.txt
```

버튼 액션은 저장 위치가 확정된 행만 삽입하며, 미확정 조사 행은 `exports/field_action_inventory.csv`에서 `UNPROVEN`으로 유지한다.

여러 세션이 동일한 게임 파일을 수정할 때 세션별 바이너리 완성본을 순서대로 덮어쓰지 않는다. 반드시 하나의 CLEAN 기준본에 논리 패치를 누적 병합한다.

```text
FIELD.BIN           ← SYSTEM + STATUS + FIELD_NAME
SYS/GR3.MDZ/GR3.MDT ← ITEM + BATTLE/SKILL/MAGIC + SKILL_EFFECT + CHARACTER + BATTLE_HELP + ENEMY + FONT
DATA/*.MDZ/*.MDT    ← SCENARIO + NPC_DIALOGUE + 위치가 확정된 FIELD_ACTION
```

ISO 사전검증은 적 이름, 필드 이름, 전투 연출 도움말에 대해 다음을 별도로 확인한다.

```text
CSV 행/ID 보존
kr_encoded_hex의 UNASSIGNED 잔존 여부
required glyph 통합 여부
대상 레코드/고정 슬롯 반영 여부
ISO 역추출 후 작업 후보와 일치 여부
PCSX2 전투 적 이름 및 맵 이동 필드 이름 화면 확인
전투 연출 도움말 전체 확정 레코드 수와 ID 보존
전투 연출 도움말 줄 구분(1F 00), 종료 코드(1A 00), 제어코드 보존
PCSX2 전투 연출 도움말 화면 확인
```

원본 추출 데이터는 직접 수정하지 않는다.

모든 테스트는 CLEAN baseline에서 다시 생성한다.

---

# 4. 전체 프로젝트 Phase

```text
PHASE 0   한글 출력 경로 실증
PHASE 0.5 중앙 Translation Manager 기반 구축
PHASE 1   시스템 메뉴 / 시스템 메시지 한국어화
PHASE 2   상태창 / 장비 화면 한국어화
PHASE 3   아이템 이름 / 설명 한국어화
PHASE 4   전투 UI / 스킬 / 마법 / 메시지 한국어화
PHASE 5   시나리오 전체 전문 번역
PHASE 6   전체 번역문에서 실제 사용 한글 음절 집계
PHASE 7   최종 Hangul code ↔ glyph table 고정
PHASE 8   시나리오 전체 게임 삽입
PHASE 9   최종 폰트 디자인 / 전체 QA
```

현재 각 작업은 세션별로 병렬 진행할 수 있지만 **폰트 코드/글리프 최종 배정은 중앙 세션만 담당한다.**

---

# 5. 세션 분업 구조

세션별 상세 지침은 `docs/session/` 아래 문서를 따른다.

```text
CENTRAL   → 중앙 DB / GUI / merge / glossary / font map / final build
SYSTEM    → 시스템 메뉴 및 시스템 메시지
STATUS    → 상태창 / 장비창
ITEM      → 아이템 이름 / 설명
BATTLE    → 전투 / 스킬 / 마법
BATTLE_HELP → GR3.MDT 전투 연출/튜토리얼 도움말 전수조사·번역
ENEMY     → 적 이름 전용 테이블 번역 / 검수 / 필요 글자 집계
FIELD     → 필드 이름 고정 테이블 / 버튼 액션 위치 조사 및 번역
SCENARIO  → DATA/*.MDZ 시나리오 추출 / 전문 번역
NPC       → DATA/*.MDZ 일반 NPC 대사 / NPC 화자명 추출·번역
FONT_PROOF→ 한글 출력 구조 1차 실증
```

각 작업 세션은 다음만 책임진다.

```text
일본어 원문 정리
한국어 번역
고유 ID 부여
source/offset/pointer metadata
자기 번역에서 필요한 한글 음절 목록
표준 CSV export
```

각 작업 세션은 다음을 독자적으로 하지 않는다.

```text
신규 한글 code 최종 배정
일본어 glyph slot 최종 선택
최종 font table 수정
최종 GR3.MDT 폰트 누적 patch
```

이것은 중앙 세션의 책임이다.

---

# 6. 중앙 Translation Manager

프로젝트 번역의 Source of Truth는 최종적으로:

```text
translation.db
```

로 한다.

권장 구현:

```text
Python
PySide6 또는 CustomTkinter
SQLite
```

GUI 탭:

```text
시스템
상태/장비
아이템
스킬/마법
전투
적 이름
필드 이름/액션
시나리오
기타
전체검색
```

기본 목록:

```text
ID | 일문 | 번역문 | 상태 | 인게임확인
```

모든 문자열에는 offset과 별개의 고정 ID를 부여한다.

예:

```text
SYS_MENU_0001
STATUS_PARAM_0003
ITEM_0107_NAME
ITEM_0107_DESC
BATTLE_CMD_0012
BATTLE_HELP_0001
ENEMY_0131_NAME
FIELD_NAME_0001
SKILL_0034_NAME
SCN_D0123_0042
NPC_D1_00030000_0001
```

hex offset 자체를 ID로 사용하지 않는다.

기존 legacy CSV는 버리지 않는다.

중앙 세션에서 inventory 후 import adapter를 만들어 공통 schema로 변환한다.

---

# 7. 공통 문자열 Schema

권장 schema:

```text
id
category
sub_category

source_file
inner_file

record_id
scene_id
string_index

original_offset
pointer_offset

jp_raw_hex
jp_text

kr_text
kr_encoded_hex

speaker
control_codes

status
game_verified

translator_note
review_note
```

상태값:

```text
UNTRANSLATED
TRANSLATED
REVIEW_1
FINAL_REVIEW
INSERTED
GAME_VERIFIED
NEEDS_FIX
```

중앙 한글 map에 없는 글자가 있으면 `kr_encoded_hex`를 임의 생성하지 말고 `UNASSIGNED`로 둔다.

---

# 8. 폰트 / 글리프 전략

## 핵심 전략

처음부터 한글 2350자 또는 11172자를 새로 확장하지 않는다.

우선:

```text
게임 원문에서 사용되지 않는 기존 일본어 custom code/glyph
→ 한글 음절로 재활용
```

한다.

이 방식의 목적:

```text
GR3.MDT 크기 증가 최소화
MDZ 구조 변화 최소화
glyph count 유지
renderer 수정 최소화
ISO/LBA 영향 최소화
```

일본어 원문 전체에서 code 사용 횟수를 계산한다.

```text
usage_count == 0 → FREE
usage_count 1~2  → LOW_USAGE
그 이상         → KEEP
```

FREE slot부터 사용한다.

LOW_USAGE는 해당 일본어 원문이 모두 한국어로 교체되어 원 glyph가 더 이상 필요 없는 경우에만 검토한다.

## 중앙에서만 관리할 파일

```text
japanese_glyph_usage.csv
free_glyph_slots.csv
hangul_code_map.csv
hangul_glyph_map.csv
korean_required_glyphs.txt
```

한 번 `GAME_VERIFIED` 또는 `FINAL`로 확정된 한글 코드 매핑은 가능한 한 변경하지 않는다.

신규 글자는 기존 mapping 뒤에 추가한다.

---

# 9. 폰트 디자인 시점

**폰트 구조와 폰트 디자인을 분리한다.**

초기에는 단순한 테스트 비트맵을 사용해도 된다.

먼저:

```text
code
→ glyph
→ actual rendered Hangul
```

을 증명한다.

최종 디자인은 PHASE 9에서 처리한다.

최종 후보로 Neo둥근모를 사용할 수 있다.

필요하면 신뢰 가능한 배포처에서 폰트를 확보하고 라이선스를 확인한다.

최종 단계에서는:

```text
code mapping 유지
glyph slot 유지
문자열 유지
bitmap만 최종 디자인으로 교체
```

하는 것이 원칙이다.

---

# 10. PHASE 0 — 최초 실증 목표

첫 목표:

```text
やくそう
→ 약초
```

현재:

```text
やくそう
→ 家くそう
```

까지 실기 확인되어 있다.

다음은 실제 live glyph source를 증명해야 한다.

과거 특정 TEX1/ordinal/atlas 위치를 추정해 destructive patch했으나 화면이 변하지 않은 테스트가 있으므로, 실패한 후보를 재사용하지 않는다.

무검증 가정 금지:

```text
lookup result == glyph ordinal
glyph_index % N == atlas coordinate
보이는 PNG 좌표 == raw bitmap offset
```

한 번에 한 변수만 바꾼다.

PCSX2에서 실제 변화가 없으면 해당 가설은 REJECTED/UNPROVEN이다.

---

# 11. PCSX2 Debugger 협업 규칙

Codex가 PCSX2를 직접 조작하려 하지 않는다.

런타임 확인이 필요하면 사용자가 직접 측정하고 Codex는 **무엇을 확인할지 정확히 지정**한다.

요청 형식:

```text
[PCSX2 확인 요청]

목적:
...

게임 상태:
...

검색 byte pattern / address:
...

검색 범위:
...

Breakpoint / Watchpoint:
...

게임에서 할 행동:
...

알려줄 값:
...
```

예:

```text
목적:
Item 107 문자열 RAM 위치 확인

검색 bytes:
8A F0 62 70 59 00

게임 상태:
아이템 메뉴에서 해당 항목 표시

결과:
찾힌 EE RAM 주소만 전달
```

가능하면 한 번의 사용자 측정으로 여러 가설을 제거하도록 분석 지점을 선택한다.

PCSX2 조작법 자체를 장시간 연구하거나 사용자에게 복잡한 절차를 요구하지 않는다.

---

# 12. 각 세션의 한글 문자 처리

각 작업 세션은 자기 `kr_text`에서 실제 사용된 한글 완성형 음절을 unique set으로 추출한다.

예:

```text
system_required_glyphs.txt
status_required_glyphs.txt
items_required_glyphs.txt
battle_required_glyphs.txt
battle_presentation_help_required_glyphs.txt
enemy_names_required_glyphs.txt
field_names_required_glyphs.txt
scenario_required_glyphs.txt
npc_dialogue_required_glyphs.txt
```

중앙 세션이 이 파일을 모두 합친 뒤 최종 code/glyph table을 만든다.

즉:

```text
각 작업 세션 = 어떤 한글이 필요한지 결정
중앙 세션     = 그 한글에 어떤 code/glyph를 배정할지 결정
```

---

# 13. 시나리오 전략

기존 분석과 선행연구상 `DATA/*.MDZ`가 주요 scene/scenario resource 후보다.

시나리오는:

```text
전체 추출
→ 전문 번역
→ 문맥 검수
→ 용어 통일
→ 전체 필요한 한글 집계
→ 최종 font/code map 고정
→ 게임 삽입
```

순서로 진행한다.

한 줄 번역 후 즉시 삽입하는 방식은 피한다.

---

# 14. 번역 검수 / 수정

Translation Manager에는 최소 다음 기능을 만든다.

```text
탭별 보기
전체 검색
ID / 일문 / 번역문
상태
인게임 확인
수정 저장
CSV import/export
필요 한글 집계
```

추가 권장:

```text
[수정 요청 복사]
[소스 위치 복사]
```

수정 요청 예:

```text
ID: SCN_D0142_0021
구분: 시나리오
일문: ...
현재 번역: ...
수정 요청:
```

나중에 사용자가 ChatGPT/Codex에 ID만 전달해도 어느 문자열인지 바로 찾을 수 있어야 한다.

---

# 15. 토큰 / 시간 절약

하지 말 것:

```text
이미 해결된 구조 재탐색
파일 전체 hex dump를 대화에 출력
수천 줄 disassembly 출력
근거 없는 대규모 brute-force
실패 방법 반복
동일 데이터 중복 export
```

분석 로그는 파일로 저장하고 대화에는 결론 중심으로 보고한다.

권장 보고:

```text
WHAT WE KNOW
WHAT CHANGED
TEST RESULT
NEXT STEP
```

---


# 16. Git / GitHub 작업 규칙

Git은 **소스 코드, 문서, 번역 데이터, 분석 결과, 재현 가능한 도구**를 관리하기 위해 사용한다.

게임 원본 데이터나 저작권이 있는 원본 게임 파일을 저장소에 올리는 용도로 사용하지 않는다.

## 절대 Git에 추가하지 않을 것

다음 종류의 게임 원본 및 원본에서 직접 추출한 대용량/저작권 파일은 Git에 절대 추가하지 않는다.

```text
원본 ISO
원본 BIN
원본 MDZ
원본 MDT / DAT
원본 DATA 파일
원본 실행 파일/게임 데이터
원본 디스크 추출 폴더
PCSX2 dump / memory dump
저작권이 있는 원본 폰트 파일
```

특히 다음과 같은 경로는 저장소 추적 대상에서 제외하는 것을 권장한다.

```text
original_disc/
original_iso/
build/
tmp/
work/
extracted/
dump/
pcsx2_dumps/
*.iso
*.bin
*.mdz
*.mdt
*.dat
```

단, 프로젝트에서 직접 만든 **작은 테스트 fixture 또는 구조 검증용 최소 샘플**을 저장할 필요가 있다면 원본 게임 데이터를 그대로 포함하지 말고, 재배포 가능한 형태인지 사용자가 명시적으로 승인한 뒤 별도 처리한다.

## 작업 시작 전

모든 의미 있는 작업을 시작하기 전에:

```bash
git status
```

를 확인한다.

확인 목적:

```text
현재 branch
미커밋 변경사항
예상하지 못한 파일
원본 게임 파일이 실수로 staging/tracking 되었는지
```

사용자의 기존 변경사항이 있으면 임의로 삭제하거나 되돌리지 않는다.

## 작업 중

중요한 단계가 끝날 때마다:

```bash
git diff
```

를 확인한다.

staged 변경도 확인할 필요가 있으면:

```bash
git diff --staged
```

를 사용한다.

목적:

```text
의도한 파일만 변경되었는지
바이너리/원본 게임 파일이 들어오지 않았는지
자동 생성 파일이 과도하게 포함되지 않았는지
기존 데이터가 실수로 삭제되지 않았는지
```

## Commit 원칙

의미 있는 작업 단위로 commit한다.

좋은 예:

```text
feat: add item translation importer
docs: document GR3 custom encoding findings
fix: correct item description pointer offset
feat: add translation manager search filters
data: normalize battle translation schema
```

서로 관련 없는 변경사항을 하나의 거대한 commit으로 합치지 않는다.

commit 전에:

```bash
git status
git diff
git diff --staged
```

를 다시 확인한다.

## Push 원칙

**사용자가 배포 가능 상태라고 판단한 단계에서만 push한다.**

Codex가 임의로 작업 도중 계속 push하지 않는다.

push 전에는:

```text
git status
git log -n 5 --oneline
```

등으로 현재 상태를 확인하고 사용자 의도와 일치하는지 확인한다.

## 금지

다음은 사용자의 명시적 요청 없이 하지 않는다.

```text
git push --force
git push --force-with-lease
git reset --hard
git rebase -i
git filter-branch
git filter-repo
과거 commit 삭제
branch history rewrite
remote branch 삭제
tag 삭제
```

즉:

```text
강제 push 금지
기존 Git 히스토리 임의 삭제 금지
기존 Git 히스토리 rewrite 금지
```

를 기본 원칙으로 한다.

## GitHub 저장 대상

권장:

```text
AGENTS.md
docs/
tools/
scripts/
translation manager source
schema
translation CSV
glossary
codebook
재현 가능한 patch scripts
분석 결과 MD/CSV
README
.gitignore
```

주의:

번역 CSV나 codebook에 원본 게임 텍스트가 포함되어 공개 배포에 문제가 될 가능성이 있는 경우, 공개 GitHub 저장소에 push하기 전에 사용자가 배포 범위를 판단한다.

## 자동 생성 빌드 산출물

다음은 기본적으로 Git에 넣지 않는다.

```text
test ISO
patched ISO
repacked MDZ
temporary MDT
cache
Python __pycache__
GUI build output
logs
```

재현 가능한 script와 source data를 저장하고, 빌드 결과물 자체는 필요할 때 로컬에서 다시 생성하는 방향을 우선한다.

## 각 세션의 Git 책임

각 작업 세션은 자기 변경사항만 commit 가능한 상태로 정리한다.

중앙 세션은 여러 세션의 결과를 merge할 때:

```text
ID 충돌
schema 충돌
중복 파일
동일 파일 동시 수정
```

을 먼저 검사한다.

동시에 여러 세션이 같은 중앙 파일을 직접 수정하지 않는 것을 원칙으로 한다.

특히 다음 중앙 관리 파일은 CENTRAL 세션 중심으로 관리한다.

```text
translation.db
hangul_code_map.csv
hangul_glyph_map.csv
free_glyph_slots.csv
glossary.csv
```

작업 세션은 해당 파일의 변경을 직접 확정하기보다 자기 결과물을 제출한다.


# 17. 새 세션 시작 절차

새 세션은 먼저 이 `AGENTS.md`와 Reference Pack을 읽는다.

그 다음 현재 세션의 역할 문서를 `docs/session/`에서 읽는다.

그리고 기존 자료를 토대로:

```text
docs/SESSION_BASELINE.md
```

를 만들되 장황하게 다시 분석하지 않는다.

그 후 해당 세션 역할의 실제 작업으로 바로 진입한다.

---

# 18. 프로젝트 완료 기준

최종 성공은 문서가 아니라 PCSX2에서 판단한다.

한국어 문자열이:
- 정상 출력
- UI에서 깨지지 않음
- 포인터/제어코드 문제 없음
- 번역 검수 완료
- 최종 ISO에서 재현 가능

해야 한다.

## 2026-08-23 런타임 회귀 방지 규칙

- `SYS/GR3.MDZ` 폰트는 원본 2,224 glyph와 FNT/SKJ/METRICS 크기를 유지한다. append-extension 후보는 런타임 검증용 중앙 빌드에 사용하지 않는다.
- 2026-08-24 중앙 고정 기준은 통합 한글 1,268자, 보호 원본 슬롯 313개, 미사용 슬롯 donor 356개, 번역 완료 일본어 donor 912개이며 `build/central-font-final-v1-integration/font-config.json`을 기준으로 한다.
- FREE-slot 감사는 단순 빈칸 추정이 아니라 SYSTEM/STATUS/EQUIPMENT/ITEM/BATTLE/SCENARIO의 실제 인코딩 사용량으로 수행한다.
- 일본어 glyph는 전부 일괄 제거하지 않는다. 제어·숫자·구두점·Latin·토큰·아직 위치가 입증되지 않은 런타임 문구는 보호한다. 알려진 모든 사용처가 한국어로 교체되는 일본어 glyph만 donor로 재사용할 수 있다.
- 번역문에 남은 `<Gxxxx>`는 원래 logical glyph index로 재출력되고 해당 슬롯이 보호된 경우 보존형 입력으로 허용한다. 의미 미해결 상태와 한글 코드 `UNASSIGNED`를 혼동하지 않는다.
- 시나리오 MDT에서 문자열 길이가 바뀌면 외부 record/chunk 크기뿐 아니라 내부 descriptor의 8-byte 단위 physical offset, declared size 및 metadata size를 함께 갱신한다.
- 일반 NPC 대사는 기존 시나리오 추출기의 `0x02/0x22` 모집단과 분리해 `NPC_DIALOGUE`로 관리한다. `0x81 0E 00 ... 0D FF 00` 계열은 원본 `DATA/*.MDZ`에서 전수조사하고, 내부 분기/오프셋 relocation 안전성이 입증되기 전에는 길이 가변 시나리오 후보를 ISO에 넣지 않는다.
- 런타임 필드명은 `FIELD.BIN` 외에 `DATA/30000000.MDZ`의 compact field-name directory도 병합 대상이다.
- 최종 교체 계획에는 `FIELD.BIN`, `BATTLE.BIN`, `SYS/GR3.MDZ`, `DATA/30000000.MDZ`, 시나리오 `DATA/*.MDZ` 전체를 포함하며 ISO 생성은 사용자 최종 승인 뒤에만 한다.

> **Workspace storage rule:** Treat `legacy/` as read-only reference/history. Do not create or modify active work products there. Save current work and generated outputs under the project root, such as `build/`, `data/`, `exports/`, or `tools/`.
